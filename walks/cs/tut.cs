((System.Func<string>)(() => { var sb = new System.Text.StringBuilder(); var ps = UnityEngine.Object.FindObjectsOfType<TutorialPopupPanel>(); for (int i=0;i<ps.Length;i++){ var p = ps[i]; if(p.IsBound && p.Shown && p.MinimizeToggle!=null && !p.MinimizeToggle.State){ typeof(TutorialPopupPanel).GetMethod("OnMinimizeCb", System.Reflection.BindingFlags.NonPublic|System.Reflection.BindingFlags.Instance).Invoke(p, null); sb.Append("minimized ").Append(i).Append("; "); } } return sb.Length==0?"tutorial already minimized":sb.ToString(); }))()
