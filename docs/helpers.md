# ES2 helper inventory

One line per helper, with the file whose doc comment carries the real contract. This file is
loaded per-need (the index in `docs/dev-loop.md` points here) — it is NOT part of the
mandatory stage read. A NEW HELPER lands here (one row) and in its own doc comment, never in
`dev-loop.md`.

| Helper | One line | File |
|---|---|---|
| `AgeLayout` | Drawn-layout reading: row banding, reading order (`ReadingOrder` across a strip, `TopThenLeft` down a page), alignment tiebreaks | `ES2Access/UI/AgeLayout.cs` |
| `DrawnTooltip` | The rendered tooltip window, read one PANEL FEATURE at a time; `Features()` is the same reading with each feature's class and the reader that answered | `ES2Access/UI/DrawnTooltip.cs` |
| `TooltipFeatures.Read` | One feature into lines: scoped row banding, repeated items, four typed readers (ship stats, fleet stats, the military power pair, a hero's card), separators skipped by the game's own flags, unknowns to the fallback | `ES2Access/UI/TooltipFeatures.cs` |
| `TooltipText` | What a row of tooltip parts SAYS — icon-as-heading vs decoration, caption+value, item strips | `ES2Access/Core/Speech/TooltipText.cs` |
| `PointerFocus` | Hover/tooltip/flyout parity for keyboard focus; `MoveTo` (button or plain transform), `MoveToToggle` (a toggle has no `SimulateHover` — its own `MouseEnter`/`MouseLeave`), `Unpoint` | `ES2Access/UI/PointerFocus.cs` |
| `GameKeyStandDown` | The input-suppression patches (mod keys win; Escape carved out); watch its counts on `/status` | `ES2Access/UI/Input/GameKeyStandDown.cs` |
| `NodeVtable.Sections` | **A control's content, declared ONCE** — an ordered list of `NodeSection` (lines + a `TooltipMode`). The engine derives BOTH surfaces from it: `TooltipParts.Part` the spoken tooltip part, `NodeBuffer.Lines` the review buffer. There is no `DetailLines` any more, and no screen wires an announcement | `ES2Access/Core/UI/Graph/GraphTypes.cs` |
| `NodeVtable.OnFollow` | **A leaf that NAMES a place already in the graph, and Right goes there** — the reference-shaped node (a starlane naming the system at its far end). `KeyGraph.TreeRight` asks it only where the node is not expandable (a group's own children win) and answers `TreeMove.Followed`, which the navigator consumes SILENTLY: the handler moves focus itself and the landing announces once, through the one path. It is NOT a click — Right is pressed speculatively all over a tree, so a handler must post no order and confirm no armed mode. It exists because the alternative — declaring the named place a second time under the referring node — breaks one-object-one-node and leaves the tree with no bottom | `ES2Access/Core/UI/Graph/GraphTypes.cs`, `KeyGraph.cs` |
| `Screen.Secondary(focused)` | Backspace offered to the SCREEN before the focused node's `OnSecondary`, mirroring `Screen.Contextual` — for a second command belonging to a PANEL rather than a control (the galaxy's way back down the lanes it has travelled). The focused node is passed so a screen answers only inside the stop it means it in; a live type-ahead search still takes the key ahead of both | `ES2Access/Screens/Screen.cs`, `ES2Access/UI/GraphNavigator.cs` |
| `TooltipParts.Part(sections)` | The spoken half: the LAST `Announce` section's words, plus "has tooltip" if ANY section is `Indicate`; `None` sections say nothing | `ES2Access/Core/UI/Graph/TooltipParts.cs` |
| `NodeBuffer.Lines` | The buffer half: a HEAD off the node's own readout (label + state words, no role/position/tooltip) — or off `NodeVtable.BufferHead` where the control declared one, for a readout that leaves out a word the buffer needs (a table cell, whose column caption is the crossed edge) — then every section in declared order, first line dropped if it only repeats the head. A node with NO sections still buffers correctly | `ES2Access/Core/UI/Graph/NodeBuffer.cs` |
| `GraphNodes.ModeFor` | The tooltip short/long rule — never pick a `TooltipMode` by hand | `ES2Access/UI/GraphNodes.cs` |
| `GraphNodes.TooltipSection` / `Sections` | A widget's tooltip as a section (mode from `ModeFor` unless overridden), and the null-dropping list builder every factory ends with | `ES2Access/UI/GraphNodes.cs` |
| `SettingRows.RowSections` | Two shapes: `(caption, value, drawn?)` for a caption-then-value row (both speak by rule, the value's wins), and `(widget, said, mode?)` for a row whose tooltips are scattered over its children — only `said` speaks, the rest are reviewable; `said` null = none speaks | `ES2Access/Screens/SettingRows.cs` |
| `AgeWidgets.Readable` | The ONE "are this tooltip's words on the widget" test (empty class or `Simple`). `ModeFor`, `TooltipLines` and every screen ask it; three private copies used to disagree about `Simple` | `ES2Access/UI/AgeWidgets.cs` |
| `AgeWidgets.Draws` | The ONE "would the game draw this tooltip" test (Content or Target — the engine's own `ReadTooltipInformation`). Aiming (`PointerFocus`) and indication (`GraphNodes.TooltipSection`) both ask it, re-evaluated per frame, so announce/aim/draw can never disagree | `ES2Access/UI/AgeWidgets.cs` |
| `GraphNodes.*` factories | **Every node is built by one.** Each ends `(tooltip, tooltipMode?, details?)` and builds `Sections` from them — `tooltipMode` null means "ask `ModeFor`", so a screen passes the tooltip and nothing else. A screen may still SET `vtable.Sections` (that IS the declaration); it can no longer wire an announcement, which is how a row used to end up with a tooltip in one surface and not the other | `ES2Access/UI/GraphNodes.cs` |
| `IconNames` + `IconTable` | Icon → `icon.*` key → name; the enumerated 382-token/408-texture table with variant aliases (one texture, `TurnSymbol`, hand-written — es2-facts) | `ES2Access/UI/IconNames.cs`, `ES2Access/Core/Speech/IconTable.cs` |
| `GalaxyViewLevels` | Which `GalaxyViewLevel` is up (`At<T>()`, `Overview`, `Scanning`, `LevelThroughTransitions`, `FocusedSystem`) + where the camera is (`ZoomStep`, `AtOrbitalZoom`, `DefaultZoomStep`, all -1/false when the galaxy camera is not the live one) and the routes (`PanTo`, `CenterOn(point, damping)` — the camera controller's own `CenterOnPoint`, called DIRECTLY for a bare point of empty map, since `PanTo` needs an entity and goes through the very GUI call `GalaxyLocate` watches for the game leading the player, `ZoomTo` — the game's own `ZoomInOnNode`, which is what a left click on a system does, `ZoomToStep` for putting a step back by hand — the mod's own zoom-undo, since the game's `RestoreZoom` can be a talking no-op (es2-facts), `ZoomForced` the map's click-zoom record, `OpenSystem`, `OpenPlanet`) and whether the view is mid-flight (`ChangingLevel` — the view's own `CanChangeGalaxyView`, true through a same-level re-entry too — and `CameraDrawing`, the camera the game's own labels measure themselves against); plus `ZoomRung`/`ZoomRungs`/`StepZoom(sign, coarse)` — one ladder from the galaxy's 13 camera steps through a system's page (rung 14) to a planet's page (rung 15), moved by the wheel's own calls; stateless, so reload-safe | `ES2Access/UI/GalaxyViewLevels.cs` |
| `Nudge` | Finishing something the game began and dropped, without fighting it: a SETTLE (how long the stall must hold before it counts as dropped rather than slow) and a PAUSE (how long to stand back after pushing, because the push is deferred too and the stall reads the same for a while afterwards). Counted in frames, engine-free, unit-tested; `PlanetOverviewScreen.FinishArriving` is the live one | `ES2Access/Core/UI/Nudge.cs` |
| `AgeWidgets` | The per-widget questions every screen asks: `Visible`/`Enabled`/`Operable` (ancestor-walking), `Raw`/`Readable`/`TooltipLines`/`TooltipTitle` (the `GuiWrapper` name behind a wordless icon), `Tooltips` (EVERY tooltip inside a widget in drawn order — for a line the mod reads as one thing that the game built out of several explained pieces), `Press`/`Toggle`/`Select`/`Choose` (replay the widget's own handler; `Toggle` flips a tick box the way its click path does, `Select` PUTS a toggle on for a group the game settles by writing every member's state back from one name — a flip there unticks the current member until the panel refreshes, which a live Selected part reads out; `Choose` takes a drop-list entry through the list's own `OnSelectionObject`/`Method`), `DoubleClick` (arity-resolved replay of the other handler, with an overload for the TOGGLE a tile hangs its second click on — a ship tile, an event popup's choice — since a toggle carries its own copy of the three double-click fields), `Offered` (the hint-button test — a blocked button stays Visible AND Enable), `Hinted` (that same control seen from the other side: it is carrying the missing-technology hint) and `Locate` (run the hint's own jump; the game gates it on the Ctrl the player is PHYSICALLY holding, so unheld it refuses exactly as the mouse's plain click does), `Point`/`PointAt` (the `(vtable, widget, tooltip)` overload aims the pointer at the widget the tooltip is ON — for a control whose tooltip the screen resolved from elsewhere; replaced three private copies) plus `TooltipOwner`, `TextOf` (a group's whole drawn phrase read off its LABELS — inline icon tokens in label text are named, a standalone icon widget is not read), `DrawnLines` (an unmodelled read-only panel as lines — tooltip text included; never a substitute for modelling a panel the player must work) and `PaintedLines` (the same reading for a panel whose lines the game FADES rather than hides — the scan view's map labels), `Painted`/`Paints` (whether the player can SEE a widget: `Painted` walks the whole chain for a faded ancestor, `Paints(parent, child)` is the engine's own one-step form (`AgeTransform.GetVisibleChildrenCount`) for a walk descending from a root it already trusts — the form to use when the ROOT itself animates, as a notification popup's own transform does on arrival), `ItemText` (an icon-only table item named from the wrapper on its own tooltip), `ChildNamed` (breadth-first, for a band the prefab names and the window never binds), `PanelTitle` (the heading the game wrote across a `GuiPanel`, off the prefab's "PanelTitle" child — no panel binds it), `Clipped` (where a widget is DRAWN, for a layout question: itself, or the scrolling window that shows it where the game laid it out BIGGER than that window — a paragraph laid out taller than its viewport keeps a rectangle that runs invisibly across everything below it, so every band/strip/row rule answers about a shape nobody can see; a widget merely scrolled out of sight keeps its own rectangle), `IndexInParent` (the repeated-node key rule's stable half), and `PressPropagating`/`ParentControl` (press the way the ENGINE does, through every wired ancestor — **benched: no live adopter yet**) | `ES2Access/UI/AgeWidgets.cs` |
| `FieldReadout.Compose` | A panel's fields as one spoken line, blanks dropped; null when there was nothing to say, which is a passive announcer's "not filled in yet" | `ES2Access/Core/Speech/FieldReadout.cs` |
| `RefusalText.Compose` | A blocked button's tooltip trimmed to the refusal itself — leading description off, the game's mouse instruction dropped | `ES2Access/Core/Speech/RefusalText.cs` |
| `GlobalHud` | The clusters drawn over every view level (empire banners, pinned quest, collapsed tutorial bar, notifications, turn controls) + the turn and pinned-quest watchers; every page under them calls `Empire`/`Quest`/`Tutorial`/`Notifications`/`Turn` in drawn order, and `Top` does the banners plus `hud:view-title` in ONE call so no view-level page can inherit half of it | `ES2Access/Screens/GlobalHud.cs` |
| `Screen.PushChild` / `RemoveChild` | Mod-owned sub-screens: one linear chain, deepest is focused, a covered parent keeps its cursor | `ES2Access/Screens/Screen.cs` |
| `DropListScreen.Open(list, title, choose)` | Any `AgeControlDropList` as a sub-screen; entries fall back to their tooltips when the list is drawn as icons, then to `EmpireColors` when it is drawn as bare swatches; the focused entry is POINTED at, so the game draws its tooltip | `ES2Access/Screens/DropListScreen.cs` |
| `EmpireColors.Name(color)` | What the player's chosen palette (`Public/Mapping/Palettes.xml`) calls a drawn colour — matched by colour, not by list position; `ModStrings` `color.*` keys, falling back to the game's identifier split at its capitals | `ES2Access/UI/EmpireColors.cs` |
| `SettingRows` + `TextFieldEditor` | One game `SettingItem` as a row (every `Gui.ControlType`, the slider's index-stepping write path, `Drawn` = visible AND alpha > 0), plus the shared row shapes every lobby-family screen builds through — `AddCombo`, `AddButton`/`AddButtonRow`, `AddTextField`, `AddReadout`, `TextFieldCell` (an edit field the game drew INSIDE a table cell) — and the deferred keyboard hand-over to a text editor | `ES2Access/Screens/SettingRows.cs` |
| `DevProbe` | Compile-checked one-liners: `Screen() Stack() State() Saves() Camera() Windows() Patches() Claims(keys?) TooltipDelay(s) Tooltip() UnknownIcons()` | `ES2Access/Dev/DevProbe.cs` |
| `DevProbe.TooltipTrace(tag)` / `TooltipPipe()` | The whole hover-to-tooltip pipeline in one line — what the mod aims the engine at, what `GuiTooltipController` remembers, its countdown (`999` = the parked request), the tooltip's class/content/target/dirty, the tooltip window, the display delay, `CanShowNotifications` and the focused node. `TooltipTrace` logs it per frame and is always false (`POST /wait`); `TooltipPipe` answers it to one poll | `ES2Access/Dev/DevProbe.cs` |
| `DevProbe.NotificationParity()` | The notification family's self-check on whichever popup is up: painted text nothing says, spoken words nothing draws, nodes filed in the wrong band or walked out of drawn order, tooltips promised or lost — four arrays, empty when `clean:true`. Both sides are re-derived (the window's own tree vs the screen's own render), and the same check runs by itself on every popup shown while the dev server is on, warning to the log (`/log?grep=parity`); the seeds that prove it bites are in `test-recipes.md` | `ES2Access/Dev/NotificationAudit.cs` |
| `DevProbe.Trace(tag)` | One LOG line per frame — stack, focused screen, cursor, that screen's declared node count, tutorial/window state — and always false, so `POST /wait` on it records a whole transition (`dev-loop.md`, "Tracing a transition frame by frame") | `ES2Access/Dev/DevProbe.cs` |
| `DevProbe.Claims("Escape,Return")` | What the input layer is claiming FROM the game: the consumed-key latch (key + still held), `backClaimed`/`claimsBack`, `layerLive` split into `screenFocused` and `keyboardElsewhere`, and `ClaimsKey`'s side-effect-free answer per named key | `ES2Access/Dev/DevProbe.cs` |
| `/input` queue | `ModInput.Inject` — actions at the production dispatch point; touches no physical key state, so game-also-sees-the-key bugs need separate link-by-link probes (`DevProbe.Claims` is the layer's end of one) | `ES2Access/UI/Input/ModInput.cs` |
| `TypeAhead` + `SearchScope` | Type-ahead as the player meets it: the letters typed, where the last result landed, and whether focus has `Strayed` off it; `SearchScope.OverStop` is the default scope (the focused stop, one result per tabular row via `NodeVtable.Column`), a screen overrides it with `Screen.TypeAheadScope` | `ES2Access/Core/UI/TypeAhead.cs`, `SearchScope.cs` |
| `GraphSheet` | The graph-native TABLE, and the only way a screen should build one: a region per section, rows down column 0, cells across, the destination column's header spoken as the crossed EDGE (so a cell never repeats its own caption) — the PRIMARY column's header included, so the step back left onto a row says "System, Dusay"; `Region(label, columns)` therefore takes the game's header list WHOLE, primary caption first and nothing dropped (a column the game draws no caption over is a null entry; a null/empty array is a plain list region, which labels nothing and is not called a table), empty cells reading `BlankText`, and no auto positions except the ROW's: every cell carries a `TableRow` (key, index, the region's row count) and the announcer speaks "3 of 12" when the player arrives in a row or moves to a different one, never while they walk that row's columns and never on the step back onto column 0. Two members exist so a screen never rebuilds a cell key by hand: `FirstRow` (the first row's primary — the id to `SetStart` on) and `Follows(id)` (continue the table below a node the screen declared itself, a lead paragraph, which then meets the first row as a row above it would). Its three static hooks are wired once in `ModEntry.InstallAnnouncerWording` (`BlankText` = `nav.cell-empty`, `TableRoleText` = `nav.table`, `TextCellType` = `ControlTypes.Text`, the one role-less type) and dropped in `Stop`; `SystemSelectionScreen` is the first adopter | `ES2Access/Core/UI/GraphSheet.cs` |
| `ResearchText` | The research screen's own phrases: an arc between two technologies said from the focused end (source vs target is the whole sentence) and all of one dot's arcs as one list (`Relationships`, which the dot SAYS - it is not buffer-only), a ring's aggregate counts, the cost/turns/queue-position readout shared by a dot and a recommended row, and which of the game's four deed-state words a ring's marker is painted in | `ES2Access/Core/Speech/ResearchText.cs` |
| `ResearchCamera` | Which view opening or closing a branch of the wheel leaves the player in — quadrant, ring, or the whole wheel (closing a stage lands in its quadrant, not at the overview); engine-free, so the rule is tested off-game | `ES2Access/Core/UI/ResearchCamera.cs` |
| `FleetOrders` | Where a fleet is (`Orbit`/`Heading`), which fleets an order would move (`Selected` — the game's own selected-garrisons repository), the route to a destination (`PathTo` a node, `PathToLink` a starlane — one pathfinding search each, so KEYPRESS-only, never per frame) and the order itself (`CanSend`/`Send`), posted the way `GalaxyGarrisonCursor.TryToMoveFleet` posts them. Every search and check takes an optional `List<FailureInfo>` — null asks "is there a route", a list asks "why not" (the game's own diagnostic ladder of relaxed re-runs). **No cancel**: the map has no such gesture — `PathToLink` on the fleet's OWN lane is the game's stop-at-the-next-node answer | `ES2Access/UI/FleetOrders.cs` |
| `RouteTurns` | The replay of `PathRenderer.DisplayMovement` off-engine: legs priced one at a time through `IRouteLegs` (a wormhole costs the whole remaining budget, a portal `min(remaining, cap)`, so costs cannot be tabled up front), answering each turn's number, end leg, arrival flag and portal/wormhole use — including the turn the renderer omits for a leg already under way (es2-facts) | `ES2Access/Core/UI/RouteTurns.cs` |
| `FleetRoute` | `RouteTurns` adapted to a live fleet: leg pricing via `IPathfindingService.GetTransitionCost` against a COPY of the fleet's shared `PathfindingData`, the in-flight leg prepended the way `RenderPath` does, and the worded answers — the preview clause ("4 turns, 21.5 movement", "Uses portal"/"Uses wormhole", or the top refusal sentence), the itinerary buffer lines (headed "Route preview" under a destination, filed under the game's own displayed turn numbers) and the committed-route clause. Memoised against a no-allocation stamp of selection/positions/points/turn, because the buffer asks the focused node every frame; `Searches` is the counter that proves it | `ES2Access/UI/FleetRoute.cs` |
| `FleetRouteWatch` | The route-loss announcer: remembers each moving fleet's destination, and when `Fleet.Path` goes null asks whether the fleet stands there — arrival silent, interception "who and where", anything else "the route was cancelled". Polling, because the game raises no event for either ending (es2-facts); baselined on start, torn down in `Stop` | `ES2Access/UI/FleetRouteWatch.cs` |
| `MapVisibility` | `Perceived(node, empire)` — the map's own rule for whether a place's name is drawn (`StarSystemLabel`'s gate: exploration ≥ 2 plus remembered-or-in-sight, the SAME for special nodes — the cursor target's ≥ 3 governs the mouse, not the name), and `Drawn(link, empire)` — a lane is offered only where the map paints it (≥ PartiallyRevealed; below that the line has intensity 0) | `ES2Access/UI/MapVisibility.cs` |
| `TradeLanes` | The drawn merge of the empire's trade-route legs into undirected lanes — the renderer's own `(min\|max<<16)` key and its accumulating blockade flag copied, not corrected — plus `Text(open, blockaded)`, the three-material phrase (open / blockaded / mixed). Engine-free; the scan view's "Trade routes" group is its one consumer | `ES2Access/Core/UI/TradeLanes.cs` |
| `FleetPanel` | The selected-fleet strip as a CONTRIBUTOR (the `GlobalHud` shape: `Baseline`/`Forget`/`Update`/`Build`), so the map stays navigable while a fleet is selected; three stops in the OWNER-APPROVED order `fleets:management` → `fleets:ships` → `fleets:actions` (a deviation from drawn order, recorded on the class), plus the arrive/leave announcement no screen change speaks for any more. A fleet line is a drop target for a carried ship (`DepartmentOfDefense.CanTransferShips` decides, `FleetsScreen.TransferShips` commits) | `ES2Access/Screens/FleetPanel.cs` |
| `CardActions` | The buttons a card or a queue line draws, as child nodes: the drawn-and-operable gate, the hinted-button refusal (declared REFUSING with the game's sentence), the mouse-instruction split and the pointer — one treatment, shared by the orbital card, the management page's card, the construction queue's buy-outs and the outpost card's action strip. A screen only says which widgets and what to call them (`AddNamedByMod`/`AddNamedByGame`/`AddNamedByTooltip`, or `AddRefusable` where the game keeps a refused button DRAWN and merely switches it off, or **`AddToggle`** where the card drew the action as a TICK — the node becomes a checkbox with the number the control writes on itself read before its state — then `Emit`; `TitleOf`/`GameText` are the two naming funcs, and `NameFromTooltip`/`FirstLine` name a control off its tooltip's first line — only where that line NAMES the thing). `Emit` also wires the missing-technology jump into an empty Ctrl slot, the same three lines `Cells.Add` wires for cells — keep the two in step, a card button never passes through `Cells.Add` | `ES2Access/UI/CardActions.cs` |
| `Cells` + `Cell` | A panel's controls gathered with the widget they were read off, then emitted in the rows `AgeLayout` says the game drew them in — hoisted out of `SystemManagementScreen`, which still calls it through its own two-line `Add`/`Emit`. `Control`/`Readout`/`AddControl`/`AddReadout` are the two kinds a panel's row can hold; `AddStat` is the captioned figure (caption from the game's registries, tooltip from the label or the group around it, `row` defaulting to the drawn group so a strip of figures in ONE group must pass its own label or the columns collide). `Add` — the one entry point taking a whole vtable — is also where the missing-technology jump is wired: on a hint-active widget it fills an EMPTY Ctrl slot with `AgeWidgets.Locate` (a screen's own Ctrl gesture wins), which is why no screen wires that gesture itself; the factory methods do NOT pass through it, so a control declared with `AddControl`/`AddReadout`/`AddStat` keeps the shared fall back to its plain click (right where the game's own handler does more than the jump under a held Control — the government Validate) — `CardActions.Emit` is the mod's OTHER declaring path and wires the identical lines. `EmitRow` is the override for a set the game WRAPS: it declares the cells as ONE row in declaration order, for a strip whose second line is the layout box running out of width rather than a new line of content (the election's representative icons, two per line); it also takes `StartRow`'s own row key and `positions` flag, for a FAMILY of such rows repeating down a panel — the election's winners share a row key (so the step between winners keeps the column: card to card, badge to badge) and switch positions off, their place said as a `NodeVtable.Row` instead, because "1 of 3" across a card and its two badges answers a question nobody asked | `ES2Access/UI/Cells.cs` |
| `ShipRows` | The ships half of a garrison, wherever it is drawn: the toolbar (named by the game's own `%Fleet*Title` action titles) and the ship tiles. `StarSystemHangarPanel` IS a `ShipsManagementPanel`, so the star system page and the fleet panel share one builder — and each inherits the buttons its own fixture never draws. A tile carries all three selection gestures (all one replayed click; the physical modifier picks the game's branch), the game's own SECOND click (Ctrl+Alt+Enter opens that ship's design — `ShipItem.OnDoubleClickCb`, wired off the tile's tick, which picks the tile out itself) and, where the caller says a drop is reachable, the pick-up (`ShipRows.ShipKind`). `Toolbar`'s `createsFleets` flag says whether this host's Create buttons make fleets, which is what the two hosts differ in | `ES2Access/UI/ShipRows.cs` |
| `GraphNodes.SelectionItem` | A row of a list the player picks SEVERAL things out of: both membership words (never the radio's silence), a `settled` reader for what to say straight after a chord when the widget's own tick lags the model, and the two chord slots left for the screen to wire | `ES2Access/UI/GraphNodes.cs` |
| `SelectionText` | What such a list says: `Membership(bool)` and `Range(names)` — the whole-selection sentence a range press answers with, null for fewer than two so the caller falls back to the row's own state | `ES2Access/Core/Speech/SelectionText.cs` |
| `ModStrings.Plural(oneKey, manyKey, n)` | A counted phrase in the form its number calls for — a whole sentence per form, never a number glued to a noun. Two forms only; a language needing three is the trigger for real plural rules in the locale file | `ES2Access/Core/Speech/ModStrings.cs` |
| `GraphNodes.RefusalPart` | A blocked control's reason as an ADDITIVE tooltip part — and only where the tooltip is merely indicated, since an announced tooltip already contains it. Read off the tooltip's own words where they exist, else off the `IFailureInfosProvider` the game hangs on the tooltip's `Target`, which is the same expression `PanelFeatureFailureInfos` draws and needs no tooltip to have been drawn | `ES2Access/UI/GraphNodes.cs` |
| `GraphNodes.HintSections` | The sections for a control whose tooltip the game may have appended a MOUSE instruction to ("hold Control+click to find the missing technology"): the words up to it speak, the instruction is reviewable only. One declaration, two surfaces, no duplication | `ES2Access/UI/GraphNodes.cs` |
| `Screen.TypeAheadScope(focused, render)` | A screen's own search scope, now handed the standing render so it can EXTEND `SearchScope.OverStop` rather than re-derive the stop (the galaxy adds every fleet buried in a closed system, parked or under way on one of its lanes) | `ES2Access/Screens/Screen.cs` |
| `CompassDirections` | Which way one place on the map lies from another, two ways. As one of eight WORDS: `Bearing(east, north)` clockwise from north, `KeyForBearing`/`DirectionKey`/`Direction` for the word; the arcs are CENTRED on the compass points (north = 337.5°–22.5°). Or as the two COMPONENTS, `Offsets(east, north)` → "23 south, 1 west" — north/south first, zero components dropped, null when both are zero (the caller says what "here" means). Hand `Offsets` the difference of two ROUNDED pairs, never a rounded difference, so the numbers agree with the pairs the player was told. Engine-free, so both are unit-tested. Named `CompassDirections` because `UnityEngine.Compass` exists | `ES2Access/Core/Speech/CompassDirections.cs` |
| `GalaxyCoordinates` + `MapCoordinates` | Where a place on the MAP is, as the pair of whole numbers said straight after its name: `Part(position)` for a fixed place and `Part(() => position)` for one that moves, both Label-kind so the pair sorts in ahead of the role word. Measured from `DepartmentOfTheInterior.HomeSystemNode` (home reads "0, 0"), origin cached and re-derived when the player empire changes identity - which is what a `/loadsave` looks like from here - and falling back to the game's own unshifted position while there is no home yet. Subtract FIRST, then round, midpoints away from zero (`MapCoordinates`, engine-free and unit-tested); no scaling, so one spoken unit is one galaxy unit and the pairs are comparable with lane lengths. `Origin()` and `Offsets(position, out east, out north)` are the way BACK - the unrounded offsets a caller asking which square of the map something is in needs, and the point a spoken pair means - and exist for the inspect cursor alone. Only map stops say the pair - not planets, not a system named on any other screen | `ES2Access/UI/GalaxyCoordinates.cs`, `ES2Access/Core/Speech/MapCoordinates.cs` |
| `InspectGrid` | The geometry of the map's inspect cursor, engine-free and unit-tested: the odd size ladder (1/3/5/7/9/11, so the cell's centre is a whole coordinate pair), HALF-OPEN cell bounds plus a step of exactly the cursor's size (which is what makes the cells TILE - no star skipped, none heard from two neighbouring cells), Liang-Barsky segment-vs-cell for a starlane crossing a cell that holds neither of its ends, the westmost-first rule that makes one lane sound like one lane from either side, the galaxy-bounds test behind the edge refusal, and `HalfWidth`/`Squares` for the per-square fog sampling | `ES2Access/Core/UI/InspectGrid.cs` |
| `GalaxyInspect` | The map's INSPECT MODE - a square of galaxy moved with the arrows and read out, which is the "what is over there" the tree cannot ask. Owned by `GalaxyHudScreen`, keys taken at MODE level through `Screen.AnyKey` (arrows, Enter, Escape, `+`/`-`; everything else falls through), Escape and the size keys claimed from the game only while `Live`. Says the cell's own pair first, then the places, fleets, probes, obliterator missiles, ally pins and crossing lanes the MAP draws there (every enumeration, visibility rule and phrase borrowed off the tree's own, none re-derived), then how much of it is under fog. Enter lands on the ONE thing in the cell and is silent for none or several; the mode cannot outlive the page, and the ending spoken from `Tick` waits for the arriving page's announcement burst to go quiet | `ES2Access/Screens/GalaxyInspect.cs` |
| `ScannerCursor` | Where a categorized scanner is POINTING and the three rules that make its keys predictable, engine-free and unit-tested: cycle to the next category/subcategory that HOLDS something (skipping empties, so a landing is never silent), step with a wrap at both ends, and re-seat on the nearest thing whenever the scope changes or the list shrank under a parked index. Each category REMEMBERS its own subcategory, so coming back to one lands where it was left (a category never visited, and one whose remembered scope has emptied, open at the first that holds something). Takes the counts as a table (one row per category, one column per subcategory) so ONE snapshot answers "is the scope next door empty"; holds no list of its own, because the lists are rebuilt per press and a position into a rebuilt list is an index, not an identity. `Arm()`/`Hold(counts, said)` are the first-press-says-where-you-are rule | `ES2Access/Core/UI/ScannerCursor.cs` |
| `GalaxyScanner` | The map's SCANNER - "what is near me, of this kind, and where is it": two categories (systems, fleets) × four scopes (all/friendly/neutral/enemy), each list rebuilt and re-sorted by distance on EVERY press and thrown away (never cached, never per-frame). NOT a mode: no arm key, nothing to exit, chords offered through `Screen.AnyKey` after the inspect cursor and simply not offered on any other page. Measures from where the player is READING - the inspect cell's centre, else the focused stop's place (`GalaxyInspect.FocusedPlace`), else home. Content and fog come off the tree's and the inspect cursor's own sources (`Galaxy.StarSystemNodes` + `MapVisibility.Perceived` minus special nodes; `FleetPresence.Drawing`); affiliation is the owner's diplomatic state, NOT the map's own three-way split (es2-facts). Says name + home pair + distance/compass + "n of m", collapsing to a "here" word at zero. Alt+Home lands the inspect cell on the ROUNDED pair, or the tree cursor on the thing's node via `GalaxyHudScreen.NodeFor` - falling back to the map's own fleet selection for a free-movement fleet the tree has no node for | `ES2Access/Screens/GalaxyScanner.cs` |
| `InspectMarker` | The square drawn round the inspect cell, drawn by the MOD in screen space: a host `GameObject` whose `OnGUI` projects the cell's four world corners through `ICameraService.Camera` each frame and strokes textured rects round the bounding box. IMGUI composites above the scene AND above the game's own windows (low `GUI.depth`), thickness and a ~26 px floor on the square are in PIXELS so no zoom can thin it away, and the colour asked for is the colour drawn — none of which the game's own line/circle/quad renderers can do (`docs/es2-facts.md` closes that saga). `Hide` DESTROYS the host, which is also its hot-reload safety | `ES2Access/UI/InspectMarker.cs` |
| `TypedText.Frame` | The characters typed this frame — the keyboard half of `GraphNavigator.TypedCharacters`; nothing while Ctrl/Alt is held, and `Input.anyKey` gates the allocating `inputString` read | `ES2Access/UI/Input/TypedText.cs` |
| `CarryState` + `CarryActions` | The keyboard's DRAG: what is held (its spoken name CAPTURED at pick-up, never re-derived), the cargo KIND that decides which controls will take it, and TWO decision tables — `Press` is Space and only ever holds things (pick up / swap / put back / silence), `Activate` is Enter and is the only thing that DROPS, through the target's OWN check, answering `NotOurs` everywhere else so the control's own click runs with the drag still live. A refusal speaks the game's words and keeps the drag. Engine-free, so both tables are unit-tested; `ModEntry.Carry` is the live one. What a control SAYS about dragging is derived from the same two vtable declarations, never written by a screen: `DraggablePart(vtable)` ("draggable", asked of the pick-up command itself — which must therefore be a pure QUERY — and silent while something is held) and `DropTargetPart(vtable)` ("drop target", live, and consulting `NodeVtable.DropAccepts` where a target family has members that refuse), both composed by `GraphAnnouncer` beside "has tooltip" | `ES2Access/Core/UI/Carry.cs` |
| `GameKeyboardHandover` | Keeps the game's focused control from acting on a key the mod already spent this press (the rename box that opened and committed in one frame — `TookTheValidateKey`), and takes the keyboard back from a key-exclusive control the game hid without unfocusing; Install/Remove beside `GameKeyStandDown`, Tick from the pump before `Input.Tick` | `ES2Access/UI/Input/GameKeyboardHandover.cs` |
| `TableSheet` | A `GuiTable` read as a `GraphSheet`. `Headers` declares the sort band in the table's OWN stop, one row above the first line (Up from row 1 reaches THAT column's heading and Down comes back to that column's cell — the band stamps each heading with its column number and `GraphBuilder`'s mode-boundary stitch pairs the seam by it — Enter still sorts) and with no positions on it — "1 of 8" there counts columns; `Rows` then tells the builder the stop lands on the first ROW (`GraphBuilder.LandStopOn`), so Tab never opens the table on the sorted column's heading, which reads "selected" like a chosen row. Read-only and refusing are DIFFERENT flags (can-select = `LinesTable.Enable`; a refused row = the line's own `Enable`), and `RowsAreLines` is the override for a table bound selectable whose window wired nothing to the pick (the journal) — rows that say "not selected" there offer a choice the window does not have. The table's own DOUBLE click is wired here for every table and every cell (`ShowOnMap`: the line's `DoubleClickButton`, row selected first) — a screen never declares it; the save list's acts-not-shows second click (load/overwrite, behind the game's own confirmation) is carried like the rest, owner-ruled 2026-08-14. A selectable row speaks its selection state with NO role word (`ControlTypes.Text` — the radio word was noise on a table row, same ruling). Seven hooks: ReadCell, SplitCell (one game cell read as several columns, each with its own Enter — the journal's Details buttons; parts declare their own availability, so the row's is not said twice), ReadValue (ONE answer serving the cell, its buffer and the row summary), ActivateCell, Decorate (a gesture on some control of the row's OWN, not the table's — the marketplace steppers), NameColumn, RowDetails, CellTooltipReading (a column whose tooltip the shared `ModeFor` rule reads wrong — the save list's Mods dossier; key it on `PropertyOf(header)`, the game's own column name, never the translated caption). Every cell declares its buffer's HEAD as caption+value (`NodeVtable.BufferHead`), which is also the first of its own facts, so the pair is said once however the cell was read; first adopter `SystemSelectionScreen` | `ES2Access/UI/TableSheet.cs` |
| `HeroCards` | Hero-card bands in `Refresh` order, gated on the game's `Display*` flags; `Wired(card, handler)` finds a button by what it DOES; god-mode handlers are never offered | `ES2Access/UI/HeroCards.cs` |
| `SidePanels` | One stop per DRAWN side panel, shared across the icon-strip screens; `Content` is the panel-body variant. Known gap: it drops the panel title's own explanatory tooltip (roadmap) | `ES2Access/UI/SidePanels.cs` |
| `SystemPanels` | The constructibles / queue / hangar panels read off a panel INSTANCE plus a key prefix (the star system page and the empire page draw their own): queueing, cancel, buy-outs and the carry-reorder live here, while layout, stop order and naming stay with the screen — and an instance one page leaves with a null observer is the trap | `ES2Access/UI/SystemPanels.cs` |
| `LawCards` | The senate family's law and government cards as nodes (bands, costs that live INSIDE the control they belong to, and the model gate that stops an emptied card speaking its old words) | `ES2Access/UI/LawCards.cs` |
| `SystemLabelReadout` | A galaxy system label's readout taken off its widgets and their `GuiWrapper`s, never off the simulation — and never off a hidden pooled widget, which holds the PREVIOUS system's values. Also reads deposit exploited-state, the two garrison ship counts, the Academy standing and the KOTH row sentence | `ES2Access/UI/SystemLabelReadout.cs` |
| `GalaxyLocate` | The game-wide "go and look at this": a Harmony capture of the three `GuiManager` reveal calls (entity, position, quest pin), one-shot `Peek`/`Take`/`Forget` like `ResearchLocate`, plus `Suppressed` for the camera moves the MOD makes through the same calls. `GalaxyHudScreen.FollowTheGame` (in `OnUpdate`, not `OnPush` — a reveal fires with the page already up) resolves it to a node: the named thing first, else the nearest declared place to the point within `LocateRadius`, else the view-title landing with "Shown on the map". It SUBSUMES the old fleet-arrival block (`_arriveOn`/`_leftHolding` selection diffing) | `ES2Access/Screens/GalaxyLocate.cs`, `ES2Access/Screens/GalaxyHudScreen.cs` |
| `NearestPick` | "Which of these was meant", by squared distance: nothing outside the radius is an answer, nearer wins, and a TIE goes to whoever was offered first — which is how a caller expresses a preference between things that genuinely share a position (a star and the fleets parked at it) | `ES2Access/Core/UI/NearestPick.cs` |
| `GalaxyHudScreen.FollowCamera` | The pattern-bearer for a camera-dependent focus visual: `Navigator.ClearVisual()` from the screen's `OnUpdate` when the camera state changes — re-committed the same frame, no flicker (`OnFocusVisual` fires once per focus CHANGE, so the camera can move an anchor off-screen under a standing cursor) | `ES2Access/Screens/GalaxyHudScreen.cs` |
| `ShipDesignRows` / `ShipDesignScreen` | The designer's module tiles (double-click-only in the game, so Enter stays silent and the chord carries it), the hull slots, and the drag committed through the game's own `ApplyDrop` without ever starting a drag | `ES2Access/UI/ShipDesignRows.cs`, `ES2Access/Screens/ShipDesignScreen.cs` |
| `StepWatch` | A wizard's step as a watermark: baseline on arrival, commit only on the frame that actually spoke | `ES2Access/Core/UI/StepWatch.cs` |
| `BurstWatch` | The same discipline for a BURST of events (battle narration): per-run aggregation, one watermark per run | `ES2Access/Core/UI/BurstWatch.cs` |
| `RungWatch` | The same discipline for a LADDER that exists wherever the player is standing (the zoom): first sighting is never news (there is no arrival to baseline against), a rung the focused slider has just read out is absorbed silently, and a value still settling is not an answer at all | `ES2Access/Core/UI/RungWatch.cs` |
| `GraphNodes.AddRefusal` | A blocked control's reason as an additive part, with a reason that merely repeats the control's own name suppressed | `ES2Access/UI/GraphNodes.cs` |
| `BattleText` / `BattleRosters` | The battle narrator's phrases (tiers, aggregation, the nine end-of-battle words) and the setup popup's roster reader | `ES2Access/Core/Speech/BattleText.cs`, `ES2Access/UI/BattleRosters.cs` |
| `EmpireDossier`, `DiplomacyActions`, `NegotiationTerms`, `WindowShape`, `DiscoveryCards.Read`, `OptionalText.Phrase` | The diplomacy family and the screen-sweep helpers — an empire's dossier, the action strip, negotiation terms, a shape reader for a surface not worth modelling widget by widget, discovery cards, and "say this only if there is something to say" | `ES2Access/UI/` (one file each), `ES2Access/Core/Speech/OptionalText.cs` |
| `SessionChat` | Everything said in a session, spoken — and in a multiplayer one that is the session's whole life, because the game posts joins, leaves, kicks, host changes, renames, the launch countdown and the clock sync as SYSTEM chat messages, so one subscription narrates all of it; queued, with a review buffer of its own. `HasChat()` is the one gate every chat surface asks (any MP session, plus any running game — the in-game panel is live in single player too), `Panel()` the drawn in-game panel, `Messages()` the game's own history and `Line(message)` the words one message is said in, so the narration, the buffer and the walkable log can never diverge | `ES2Access/UI/SessionChat.cs` |
| `SaveProgress` | The one thing the game reports with no words at all: a save in flight (the spinner). Watches the `IsSaving` FLAG, not the window — the window also needs `GameReady`, which a save written during start-up does not have — and speaks queued at both ends; a failed save clears the same flag, and the game's own error dialog is what tells the two apart | `ES2Access/UI/SaveProgress.cs` |
| `FleetPresence` | Which fleets the map DRAWS at a place: `At`/`On` (a system's docking slots, a lane's in-flight legs) as the game's own relation-aware count phrase (`GuiFleetGroup.Title`), `LinesAt`/`LinesOn` the same groups with their fleet names for the buffer, `FleetsAt`/`FleetsOn` the fleets THEMSELVES (the hangar garrison dropped — it is counted but is not a fleet), which is what the tree hangs under a place, and `Drawing()` every fleet the map draws ANYWHERE (for a caller asking about a REGION with no node and no link to ask through — the inspect cursor; a parked fleet's `GalaxyPosition` is its star's, so it lands in the star's square). Walks the two label windows' own repositories, so no vision rule is reimplemented. The count phrases never run from `Build` (`ValuePart(..., watch: false)`); the enumerations do, but only for a place the player has OPENED. A fleet under way is hung under BOTH end systems (never under the lane, which is a leaf now) off `GalaxyHudScreen.LanesOf` — the one lane list the lane NODES, the en-route fleets' "on starlane 2, west" part and the system's "1 fleet under way nearby" clause all number themselves from, so those three cannot name different lines. `ShowsShipCount(fleet)` is the separate permission to know how BIG a fleet is — the lozenge's own test (`GarrisonsLabelButton.RefreshShipCount`: automated, or visibility ≥ Visible), which callers use to OMIT the "N ships" part rather than to fill it with a placeholder; `Selectable(fleet)` is the third permission — the cursor target's own refusal of an AUTOMATED fleet (`GalaxyFleetCursorTarget`, which withholds highlight as well as selection), which makes a fleet row declare itself `ControlTypes.Text` with no action instead of a button that does nothing | `ES2Access/UI/FleetPresence.cs`, `ES2Access/Screens/GalaxyHudScreen.cs` |
| `CursorTargeting` | The game's two-step targeting modes: `Aiming` (a cursor is waiting for a target — the game's own `HasUserInstructions`), `ConfirmAt(node)` (the game's own left click on that node; target-aimed cursors go through their own handler, the probe and the coordination pin through the order they post), `Contextual()` (the armed mode's own RIGHT click — cancel, waypoint-back or prompt-close, decided by the cursor; no node needed, no right branch reads one) and `EscapeIsOurs` (true only for `TakeSystemCursor`, the mode the game left without an Escape). `ConfirmAt(lane, far)` is the same first refusal for a starlane node (pointer-aimed modes aim at the far extremity, flipped when the acting fleet stands on it; target-aimed modes get the lane's own link target, which every targeting cursor refuses silently). `ArmedProbe`/`ConfirmTowards(bearing)` are the probe mode's third confirm shape — a free compass aim, since the game refuses only a zero vector. `PreviewLines(node)` is the armed mode's HOVER half: what the waiting order says about this place, replayed through the cursor's own `OnCursorEnter` and read off the overlay EVENT — buffered, never spoken; empty when no mode is armed or the mode writes nothing | `ES2Access/UI/CursorTargeting.cs` |
| `GameChatKey` | Moves the game's chat key off the mod's keys at startup — only while it is still the shipped Return/Tab default — and hands whichever chord the binding sits on back to the game (follows a re-bind; repairs after an options reset) | `ES2Access/UI/Input/GameChatKey.cs` |
| `ZoomLadder` | How close the game is looking, as a slider the player can move — one rung from the whole galaxy to one planet, on the arrows the mod already spends on a value, because the game's own keyboard zoom is two HELD keys and nothing at all inside a system. One instance per page (`Build`/`Update`/`Forget`, so a hot reload is clean); the galaxy's view-title stop and the scan view each carry one, and what a rung MEANS is the page's business, not the ladder's. `IsLadder(id)` answers "is the cursor already being told the rung", which is how `ZoomWatch` avoids saying it twice | `ES2Access/UI/ZoomLadder.cs` |
| `ZoomWatch` | The same rung said out loud wherever the player is standing, however it moved — the wheel, the game's held zoom keys, a click that flies into a system, the pages a system and a planet open onto. Pump-scoped (`ModEntry`), because the zoom CROSSES pages and a watcher owned by one of them would fall silent on the step that changed the page; queued, and silent on the ladder itself | `ES2Access/UI/ZoomWatch.cs` |
| `GraphNavigator.CursorMovedHere` | Whether the focus visual being committed is the cursor having MOVED, as opposed to a page being re-entered or the visual re-taken where it already was — asked from inside `OnFocusVisual` by every hook that moves the WORLD (the galaxy's camera pans), so re-entering a page reflects where the game is looking instead of flying the camera back to the node the player left | `ES2Access/UI/GraphNavigator.cs` |
| `ChatScreen` | The in-game chat panel as a mod-owned CHILD screen pushed over whatever page the player is on (no layer — the manager focuses the deepest child): the recipient tabs (Global/Alliance — the tab decides who the next typed message goes to), the message log as an expandable group of the newest fifty (the panel's own cap; the review buffer keeps the rest), and the box itself, whose Enter is the game's `SetFocus`. Tab cycles inside it and the page underneath is unreachable, which is what open chat is for a mouse too; `AnswersOnly`, so nothing shared is added. Declares NOTHING while the game holds the keyboard for the box, so opening chat sounds like one thing. `BuildNewMessages` is the page-level half — the new-message button, the only part the game draws while chat is CLOSED — contributed through `Screen.BuildShared` | `ES2Access/Screens/ChatScreen.cs` |
| `ChatHold` | The mod's hand on the in-game panel: pushes/pops `ChatScreen`, and holds the panel non-discreet for as long as that page is up — which is what a pointer resting on the panel does for a mouse, and the only reason the mod can gate chat on the DRAWING at all. `IsOpen`/`Typing` are the game's own state (the private `discreet` field, the field holding focus); `StepOutOfTyping` is the first Escape, `Close` the second, `ChildClosed` every other ending (the game's own toggle, an empty validate, the page under it going, teardown) — the panel is let go the game's way, which leaves it open under a resting pointer | `ES2Access/UI/ChatHold.cs` |
| `ChatEscape` | Harmony prefix on `InGameChatPanel.HandleInput`: Exit delivered to the panel while its box has the keyboard becomes a step OUT of the box instead of the game's close-the-whole-panel. Has to be there and not on the `AgeControlTextField.KeyDown` seam — the InputManager's Update-time dispatch closes the panel before the engine's LateUpdate KeyDown route ever runs; matches the ACTION, so a re-bound Exit is covered | `ES2Access/UI/Input/ChatEscape.cs` |
| `ChatField` | Announces the game's chat box taking the keyboard ("Chat. Type a message…") — for the chat key and for Enter on the box node alike, which is why that node announces nothing of its own; scoped by `ChatPanel` identity, not control type (a third chat-type field is the coordination-request pin box). Only the LOBBY's field is reported on the way OUT (by re-reading the cursor's control): the in-game panel has `ChatScreen`, whose arrival and departure already say where the player landed | `ES2Access/UI/ChatField.cs` |
