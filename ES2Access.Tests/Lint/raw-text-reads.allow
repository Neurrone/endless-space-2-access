# Drawn text is read through PaintedText / PaintedLines / ItemText (UI/AgeWidgets.cs), which ask what the subtree is painting, and through AgeText, which resolves keys, icon tokens and colour markup.
# A raw .Text / .TranslatedText / .Content read bypasses both: it returns the assigned string, ellipsis-free, markup and all, from a label nothing has vouched for.
# If the raw string really is what is wanted, say why at the site and record it here.
#
# Generated by ES2ACCESS_LINT_REGENERATE=1; format: path | count | source line.
#

ES2Access/Screens/BattleNotifications.cs | 1 | raw = label == null ? null : label.Text;

ES2Access/Screens/MenuDestinationScreens.cs | 1 | string.IsNullOrEmpty(name) ? AgeText.Clean(drawn.Tooltip.Content) : name

ES2Access/Screens/MinorFactionDiplomacyScreen.cs | 1 | string content = tooltip == null ? null : tooltip.Content;

ES2Access/Screens/ResearchScreen.cs | 1 | : AgeText.Clean(marker.Tooltip.Content);

ES2Access/Screens/SettingRows.cs | 1 | : AgeText.Clean(field.Label.Text);

ES2Access/Screens/SystemManagementScreen.Spaceport.cs | 1 | string content = tooltip == null ? null : tooltip.Content;

ES2Access/Screens/TextFieldEditor.cs | 1 | : (field.Label.Text ?? string.Empty);

ES2Access/UI/AgeWidgets.cs | 1 | && (!string.IsNullOrEmpty(tooltip.Content) || tooltip.Target != null);
ES2Access/UI/AgeWidgets.cs | 1 | && string.IsNullOrEmpty(tooltip.Content)
ES2Access/UI/AgeWidgets.cs | 1 | : new TooltipKey(tooltip.Class, tooltip.Content, tooltip.Target);
ES2Access/UI/AgeWidgets.cs | 1 | return label != null && !string.IsNullOrEmpty(label.Text);

ES2Access/UI/BattleRosters.cs | 1 | : AgeText.Clean(tooltip.Content);

ES2Access/UI/ModOptions/OptionTextFieldCommit.cs | 1 | : AgeText.Clean(__instance.TextFieldLabel.Text) ?? string.Empty;

ES2Access/UI/TooltipFeatures.cs | 1 | if (label != null && label.Text == key)
